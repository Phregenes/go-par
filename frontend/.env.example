# Em desenvolvimento local, deixe vazio para usar o proxy do Vite (/api → localhost:3001).
# Em produção, use a URL pública da API (ex.: https://gopar-api.onrender.com).
VITE_API_URL=

# Supabase (Auth + profiles)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
